name: Scheduled workflow
on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  every-2-mins:
    runs-on: ubuntu-latest
    steps:
      - name: Check versions
        run: |
          npm -v
          node -v
